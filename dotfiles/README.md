# dotfiles

GNU Stow packages for the environment (sway, waybar, foot, wofi, mako, nvim,
zsh, git), managed by `dotfiles.sh` (see Prompt 5 in `prompts-claude-code.md`).

Each subfolder here is a stow package: `<package>/.config/<app>/...`.
